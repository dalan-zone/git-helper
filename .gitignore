# Built artifacts
*.aab
*.apk
/build/
/app/build/
/app/prod/
.gradle/
# Keep gradle-wrapper.jar tracked — it is the small bootstrapper required by CI.
# Only ignore downloaded Gradle distributions under .gradle/.

# Local config (SDK path, signing)
local.properties
app/release.keystore
signing.properties

# IDE
.idea/
!.idea/codeStyles/
*.iml
.DS_Store

# WorkBuddy
.workbuddy/

# Version control housekeeping
# version.properties IS tracked on purpose (holds the monotonic versionCode).
